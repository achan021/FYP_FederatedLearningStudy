Folder contains 4 types of folders:
1) base_case : folder that contains the local conventional training code
2) FL_case : Federated learning code without DRL pruning
3) DRL_prune_local : DRL pruning with local conventional training code
4) DRL_prune_FL_case : DRL pruning with Federated learning
