To run local pruning. Run the DRL_pruning.py code in the respective folders. argparser --budget --dpratio
budget represents the target sparsity
dpratio is to adjust the senstivity of dynamic pruning vs static pruning. 